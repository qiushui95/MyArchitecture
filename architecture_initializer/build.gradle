plugins {
    id("com.android.library")
    id("kotlin-android")
}

android {
    compileSdkVersion(30)
    buildToolsVersion = "30.0.3"

    defaultConfig {
        minSdkVersion(21)
        targetSdkVersion(30)

        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
        consumerProguardFiles("consumer-rules.pro")
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }
    kotlinOptions {
        jvmTarget = "1.8"
    }
}

apply from: "../gradle/dependency.gradle"

dependencies {
    implementation(architectureDependency("constant"))
    implementation(architectureDependency("entity"))
    implementation(architectureDependency("model"))
    implementation(architectureDependency("getter"))
    implementation(architectureDependency("http"))
    implementation(architectureDependency("error"))

    compileOnly(rootProject.ext.dependency.Koin)
}


ext {
    PUBLISH_ARTIFACT_ID = artifactId("initializer")
}

apply from: "../gradle/jitpack.gradle"